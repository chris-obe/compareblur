{
  "name": "compareblur",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "cd app && bun install",
    "dev": "cd app && bun run dev",
    "build": "cd app && npm ci && npm run build",
    "preview": "cd app && bun run preview"
  }
}
